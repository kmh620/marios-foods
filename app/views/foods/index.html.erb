

<h1> Recently Added Foods</h1>
<ul>
  <% @foods.three_recent().each do |food| %>
    <li><%= food.name %></li>
  <% end %>
</ul>

<h1>Top Reviewed Product</h1>



<h1>All foods</h1>

<% if @foods.any? %>

  <ul>
    <% @foods.each do |food| %>
    <li><%= link_to "#{food.name}", food_path(food) %>
    <% end %>
  </ul>

  <% else %>
  <p>There are no foods to show.</p>
  <% end %>
